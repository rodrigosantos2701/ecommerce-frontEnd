import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  cardContent: {
    padding: '3%',
    minHeight: '80px',
    margin: 0,
    display: 'flex',
    
  }
}));
